%table
  %thead
    %tr
      %th ID
      %th URL
      %th Last Interaction

  %tbody
    - @arts.each do |art|
      %tr
        %td= art.id
        %td= link_to "http://#{art.url}"  , "http://#{art.url}", target: "_blank"
        %td= display_date art.last_interaction_at
